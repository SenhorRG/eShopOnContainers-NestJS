# Optional merge file for Playwright E2E (`profile: e2e`).
# Usage:
#   COMPOSE_PROFILES=e2e docker compose -f docker-compose.yml -f docker-compose.e2e.yml --env-file .env.compose.example up -d
#
# `docker-compose.e2e.yml` intentionally adds no extra services today; infra lives in the base file.

services: {}
