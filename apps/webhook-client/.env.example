# Copy to `.env.local` for Vite dev (gitignored). Do not commit real secrets.
VITE_ESHOP_IDENTITY_ORIGIN=http://127.0.0.1:5051
VITE_ESHOP_WEBHOOKS_ORIGIN=http://127.0.0.1:5055
# Prefill email only (must match apps/identity-service/prisma/fixtures/users.json).
VITE_ESHOP_DEV_LOGIN_EMAIL=webhooks-dev@eshop.local
