# Catalog product images (.webp) can grow large — enable Git LFS if your clone tracks full Pics/.
# Uncomment after installing git-lfs (https://git-lfs.com):
# *.webp filter=lfs diff=lfs merge=lfs -text
